<Window x:Class="WindowsAzure.Storage.Replicate.FrontEnd.Main"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Label Content="Source:" />        
        <TextBox Grid.Row="0" Grid.Column="1" Width="500" VerticalAlignment="Top" Text="{Binding Source, Mode=TwoWay}"/>
        <Label Grid.Row="1" Content="Target:" HorizontalAlignment="Left" VerticalAlignment="Top"/>
        <TextBox Grid.Row="1" Grid.Column="1" Width="500" VerticalAlignment="Top" Text="{Binding Target, Mode=TwoWay}"/>
        
        <Button Grid.Row="2" Padding="5" Margin="5" Grid.Column="1" HorizontalAlignment="Right" Click="replicateClick">Replicate</Button>
        <Label x:Name="Status" Grid.Row="2" Padding="5" Margin="5" Grid.Column="0" Grid.ColumnSpan="2" HorizontalAlignment="Left" Content="Ready"/>
    </Grid>
</Window>
